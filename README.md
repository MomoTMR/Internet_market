# Hop & Barley - Интернет-магазин ингредиентов для пивоварения

Проект выполнен в рамках учебного курса. Представляет собой полноценный интернет-магазин с Web-интерфейсом и REST API.

## Технологии
- **Backend:** Django 5.2, DRF
- **Database:** PostgreSQL
- **Infrastructure:** Docker, Docker Compose
- **Quality:** Pytest, Flake8, Mypy
- **Auth:** JWT (SimpleJWT)

## Структура проекта
- `config/` — основные настройки проекта, маршрутизация и WSGI/ASGI.
- `products/` — управление каталогом: модели товаров, категорий, поиск и фильтрация.
- `orders/` — корзина (session-based) и логика оформления заказов.
- `users/` — кастомная модель пользователя, регистрация и профили.
- `reviews/` — система отзывов и оценок (модели).
- `static/` — стили (CSS), скрипты (JS) и изображения.
- `templates/` — HTML-шаблоны страниц.
- `scripts/` — утилиты для импорта/экспорта данных и инициализации.

## Запуск проекта

Данное руководство описывает пошаговую инструкцию по первичному развертыванию и запуску интернет-магазина крафтового пива.

---

## 📋 Предварительные требования

Перед началом убедитесь, что на вашем компьютере установлены:

- **Git** - система контроля версий
- **Docker** и **Docker Compose** - для контейнеризации приложения
- **Python 3.10+** и **Poetry** - для локальной разработки (опционально)

---

## 🚀 Шаг 1: Клонирование репозитория

Откройте терминал и выполните следующие команды:

```bash
# Создайте папку для проекта и перейдите в неё
mkdir internet_beer_market && cd internet_beer_market

# Клонируйте репозиторий с GitHub
git clone https://github.com/MomoTMR/Internet_market.git

# Перейдите в директорию проекта
cd Internet_market
```

---

## ⚙️ Шаг 2: Настройка переменных окружения

Проект уже содержит файл `.env.example` с базовыми настройками. При необходимости вы можете изменить следующие параметры:

```bash
# Копирум файл примера и создаем новый .env файл
cp .env.example .env
# Откройте файл .env в текстовом редакторе
nano .env
```

**Основные переменные:**
- `DJANGO_SUPERUSER_USERNAME` - имя администратора (по умолчанию: `admin`)
- `DJANGO_SUPERUSER_PASSWORD` - пароль администратора (по умолчанию: `admin`)
- `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD` - настройки базы данных PostgreSQL
- `WEB_CONTAINER_NAME` - имя Docker-контейнера веб-приложения

> ⚠️ **Важно:** Для продакшн-окружения обязательно измените пароли на более надежные!

---

## 🐳 Шаг 3: Запуск проекта через Docker

### 3.1 Запуск контейнеров

```bash
# Запустите Docker Compose (в фоновом режиме)
docker compose up -d
```

Эта команда:
- Создаст и запустит контейнеры для веб-приложения и базы данных PostgreSQL
- Применит все миграции базы данных
- Соберет статические файлы

### 3.2 Проверка статуса контейнеров

```bash
# Убедитесь, что контейнеры запущены
docker compose ps
```

Вы должны увидеть два запущенных контейнера: `hopbarley_web` и `hopbarley_db`.

---

## 🎬 Шаг 4: Инициализация данных

После успешного запуска контейнеров необходимо инициализировать базу данных начальными данными.

### 4.1 Предоставление прав на выполнение скрипта

```bash
# Дайте права на выполнение скрипту инициализации
chmod +x scripts/init.sh
```

### 4.2 Запуск скрипта инициализации

```bash
# Выполните скрипт инициализации
bash scripts/init.sh
```

**Этот скрипт автоматически:**
- Создаст суперпользователя с учетными данными из `.env`
- Создаст тестовых пользователей (user1, user2, user3)
- Создаст базовую категорию "Craft Beer"
- Добавит 5 начальных товаров (IPA Classic, Stout Dark, Lager Light, Pale Ale, Porter)

---

## 🌐 Шаг 5: Доступ к приложению

После успешной инициализации откройте браузер и перейдите по адресу:

**Главная страница магазина:**
```
http://localhost:8000
```

**Панель администратора Django:**
```
http://localhost:8000/admin
```

**Учетные данные администратора:**
- Логин: `admin` (или значение из `DJANGO_SUPERUSER_USERNAME`)
- Пароль: `admin` (или значение из `DJANGO_SUPERUSER_PASSWORD`)

**API документация (Swagger):**
```
http://localhost:8000/api/docs/
```

## Работа с API (JWT)
Документация Swagger доступна по адресу: `http://127.0.0.1:8000/api/docs/`

**Пример получения доступа:**
1. **Получение токена:**
   `POST /api/token/` с данными `username` и `password`.
2. **Использование:**
   Добавьте заголовок `Authorization: Bearer <your_access_token>` к вашим запросам.

## Тестирование и Качество
- **Запуск тестов:** `pytest`
- **Линтер (Flake8):** `flake8 .`
- **Типизация (Mypy):** `mypy .`

## Чек-лист реализации
- [x] Каталог и поиск товаров
- [x] Детальная страница товара
- [x] Корзина (AJAX-добавление)
- [x] Оформление заказа и сохранение в БД
- [x] Личный кабинет (история заказов)
- [x] REST API (Products, Orders)
- [x] Swagger документация
- [x] Docker-инфраструктура
- [x] Поддержка .env конфигурации
- [x] Инструменты качества (линтеры, тесты)

## Работа с данными
- **Сохранение:** `./scripts/products.sh dump`
- **Загрузка:** `./scripts/products.sh load`
